---
// Страница «Мой профиль»: вся реализация — в компоненте ProfilePage (данные браузера + персонализация)
import ProfilePage from '../../components/ProfilePage.astro';
import { LANGS } from '../../lib/data';
export const getStaticPaths = () => LANGS.map((l) => ({ params: { locale: l === 'ru' ? undefined : l } }));
---
<ProfilePage />
